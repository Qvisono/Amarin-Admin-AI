<Window x:Class="Amarin.UI.CrashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="True"
        Topmost="True"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        Title="Amarin Admin AI — сбой">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources.xaml"/>
                <!-- Палитра: в работе — ThemeManager, в конструкторе —
                     Properties/DesignTimeResources.xaml. -->
            </ResourceDictionary.MergedDictionaries>

            <!-- Раскрывашка «Подробности»: тот же вид, что у ToolsExpander в чате,
                 но без содержимого Expander — здесь достаточно кнопки со стрелкой. -->
            <Style x:Key="CrashDetailsToggle" TargetType="ToggleButton">
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Focusable" Value="False"/>
                <Setter Property="HorizontalAlignment" Value="Left"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToggleButton">
                            <StackPanel Orientation="Horizontal" Background="Transparent">
                                <Path x:Name="Chevron" Data="M0,0 L4,4 L8,0"
                                      Stroke="{DynamicResource Text.Dim}" StrokeThickness="1.4"
                                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                      StrokeLineJoin="Round"
                                      Width="8" Height="4" Stretch="Uniform"
                                      VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5">
                                    <Path.RenderTransform>
                                        <RotateTransform Angle="-90"/>
                                    </Path.RenderTransform>
                                </Path>
                                <TextBlock x:Name="Label" Text="{TemplateBinding Content}"
                                           FontSize="11.5" Margin="7,0,0,0"
                                           Foreground="{DynamicResource Text.Muted}"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                            <ControlTemplate.Triggers>
                                <!-- Крутим сам Path, а не именованный RotateTransform: имя
                                     внутри RenderTransform в шаблоне недоступно для TargetName. -->
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Chevron" Property="RenderTransform">
                                        <Setter.Value>
                                            <RotateTransform Angle="0"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Label" Property="Foreground"
                                            Value="{DynamicResource Text.Secondary}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <!-- Отступ оставляет место тени карточки. -->
    <Grid Margin="26">
        <Border x:Name="Card" Width="520"
                Background="{DynamicResource Bg.Panel}"
                BorderBrush="{DynamicResource Border.Default}" BorderThickness="1"
                CornerRadius="12" Padding="22,20"
                MouseLeftButtonDown="Card_MouseLeftButtonDown">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="28" ShadowDepth="8" Opacity="0.5"/>
            </Border.Effect>

            <StackPanel>
                <!-- ===== ЗАГОЛОВОК ===== -->
                <StackPanel Orientation="Horizontal">
                    <Grid Width="20" Height="20" Margin="0,0,10,0" VerticalAlignment="Center">
                        <Ellipse x:Name="BadgeCircle" Fill="{DynamicResource Status.Danger}"/>
                        <TextBlock x:Name="BadgeGlyph" Text="!" FontSize="13" FontWeight="Bold"
                                   Foreground="{DynamicResource Text.OnAccent}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                    <TextBlock x:Name="HeadlineText" Text="{DynamicResource S.Crash.Headline}" FontSize="15"
                               FontWeight="SemiBold" Foreground="{DynamicResource Text.Primary}"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <TextBlock x:Name="MessageText" FontSize="12.5" Margin="0,12,0,0"
                           Foreground="{DynamicResource Text.Body}" TextWrapping="Wrap"
                           Text="{DynamicResource S.Crash.Generic}"/>

                <TextBlock x:Name="LogHintText" FontSize="11.5" Margin="0,8,0,0"
                           Foreground="{DynamicResource Text.Dim}" TextWrapping="Wrap"/>

                <!-- ===== ПОДРОБНОСТИ ===== -->
                <ToggleButton x:Name="DetailsToggle" Content="{DynamicResource S.Crash.Details}"
                              Style="{StaticResource CrashDetailsToggle}" Margin="0,14,0,0"/>

                <Border x:Name="DetailsBox" Visibility="Collapsed" Margin="0,8,0,0"
                        Background="{DynamicResource Bg.Card}"
                        BorderBrush="{DynamicResource Border.Default}" BorderThickness="1"
                        CornerRadius="6">
                    <TextBox x:Name="DetailsText" IsReadOnly="True" BorderThickness="0"
                             Background="Transparent" Padding="10,8"
                             FontFamily="Consolas" FontSize="11"
                             Foreground="{DynamicResource Text.Muted}"
                             MaxHeight="220" TextWrapping="NoWrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"/>
                </Border>

                <!-- ===== КНОПКИ ===== -->
                <Grid Margin="0,18,0,0">
                    <Button x:Name="CopyButton" Content="{DynamicResource S.Crash.Copy}"
                            Style="{StaticResource DialogSecondaryButton}"
                            HorizontalAlignment="Left" Click="CopyButton_Click"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="CloseButton" Content="{DynamicResource S.Crash.Quit}"
                                Style="{StaticResource DialogSecondaryButton}"
                                Margin="0,0,8,0" Click="CloseButton_Click"/>
                        <Button x:Name="ContinueButton" Content="{DynamicResource S.Crash.Continue}"
                                Style="{StaticResource DialogPrimaryButton}"
                                Click="ContinueButton_Click"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</Window>
