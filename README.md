<div align="center">

# 🛠️ Amarin Admin AI

**Консольный AI-агент для администрирования Windows**

Опишите проблему обычным текстом — агент сам продиагностирует и починит систему.

![Platform](https://img.shields.io/badge/platform-Windows%2010%20%7C%2011-0078D6?logo=windows)
![.NET](https://img.shields.io/badge/10-512BD4?logo=dotnet&logoColor=white)
![Arch](https://img.shields.io/badge/arch-x64-lightgrey)
![License](https://img.shields.io/badge/license-GPL%20v3.0-blue)
![Self-contained](https://img.shields.io/badge/runtime-встроен-brightgreen)

</div>

---

## 💡 Что это

Языковая модель (через **Venice.ai**) планирует действия, а локальные инструменты выполняют их прямо на вашем компьютере: PowerShell, реестр, службы, диски, брандмауэр, `winget` и многое другое.

Программа **не открывает окна настроек** и не отправляет вас в Диспетчер устройств — она делает всё сама и объясняет результат текстом.

> 🔒 Опасные операции всегда требуют подтверждения. Есть режим «только чтение» и откат последнего изменения.

---

## 📋 Требования

| | |
|---|---|
| 💻 **Система** | Windows 10 или 11 (x64) |
| 🧩 **Платформа** | Собрано на .NET 10, архитектура x64 |
| ⚙️ **Установка .NET** | Не требуется — рантайм .NET 10 встроен в `.exe` |
| 🔑 **Ключ** | API-ключ Venice.ai |

---

## 🚀 Быстрый старт

**1. Задайте ключ (один раз, навсегда)**

Откройте PowerShell и выполните — подставив свой ключ:

```powershell
setx VENICE_API_KEY "ваш_ключ"
```

Закройте это окно и откройте новое — переменная появляется только в новых процессах.

Проверить:

```powershell
echo \$env:VENICE_API_KEY
```

> ⚠️ Ключ сохраняется в вашем профиле. Не задавайте его так на чужом или общем компьютере.

**2. Распакуйте архив** `Amarin-Admin-AI-win-x64`.

**3. Запустите** `Amarin Admin AI.exe` — двойным кликом или из консоли:

```powershell
cd путь\к\папке\с\exe
.\"Amarin Admin AI.exe"
```

Готово. Введите задачу текстом, например:
> `Почему тормозит система?`
> `Проверь диск C на ошибки`
> `Какие программы можно обновить?`

Проверить инструменты без обращения к API (ключ не нужен):

```powershell
.\"Amarin Admin AI.exe" --smoke-tools
```

---

## ⌨️ Команды в консоли

Команды со слэшем выполняются локально, без обращения к модели.

| Команда | Действие |
|---|---|
| `/help`, `?` | Справка |
| `/clear` | Очистить историю, журнал, снимки и экран |
| `/session`, `/mode` | Режим: continuous / isolated |
| `/model [имя]` | Показать или сменить модель |
| `/readonly` | Режим только для чтения |
| `/undo` | Откатить последнее изменение |
| `/history` | Журнал выполненных действий |
| `/export [html\|md\|pdf\|all]` | Выгрузить отчёт сессии |
| `balance` | Баланс аккаунта Venice |
| `exit` | Выход |

### Режимы сессии

- 🔗 **Continuous** — агент помнит переписку в рамках сессии.
- 🧩 **Isolated** — каждый запрос обрабатывается с чистого листа.

---

## 🧰 Возможности

Агент сам выбирает нужные инструменты под вашу задачу.

### 🖥️ Система и диагностика

| Инструмент | Назначение |
|---|---|
| `run_powershell` | Произвольные команды PowerShell (удаление файлов заблокировано) |
| `registry` | Чтение, список, запись, удаление разделов реестра |
| `windows_service` | Список, статус, запуск/остановка/перезапуск служб |
| `filesystem` | Чтение, список, запись, копирование, перемещение (без удаления) |
| `system_info` | ОС, пользователь, время работы |
| `performance` | Загрузка CPU/RAM, uptime |
| `windows_process` | Список, топ по нагрузке, остановка процессов |
| `event_log` | Журналы событий (пресеты и произвольные запросы) |
| `wmi_query` | WMI-запросы (ограниченный набор областей) |
| `scheduled_task` | Задачи планировщика |
| `startup_programs` | Программы в автозагрузке |
| `system_repair` | Статус и запуск SFC / DISM |
| `restore_point` | Точки восстановления (≤ 1 / 24 ч). Откат — через `rstrui.exe` |
| `reliability` | Монитор стабильности системы |
| `windows_update` | Статус обновлений, ожидание перезагрузки |
| `credentials` | Сохранённые учётные данные (без паролей) |
| `devices` | Устройства PnP и драйверы |

### 💾 Диски, ПО, сеть, безопасность

| Инструмент | Назначение |
|---|---|
| `disk_management` | Диски, тома, SMART, chkdsk, статус BitLocker (без ключей) |
| `disk_space` | Анализ места, крупнейшие объекты, очистка по фиксированным категориям |
| `software_inventory` | Установленное ПО + `winget`: поиск/установка/обновление/удаление |
| `network` | Адаптеры, DNS, ping, соединения, профиль брандмауэра |
| `firewall_rules` | Правила брандмауэра: список/детали/вкл/выкл/создание/удаление |
| `dns_config` | DNS, файл hosts, прокси |
| `port_listener` | Слушающие порты и владеющие ими процессы |
| `security_status` | Состояние брандмауэра и Defender (чтение) |
| `remote_access` | Статус RDP |
| `virtualization` | Hyper-V / Docker (если доступны) |
| `windows_features` | Компоненты Windows: список/вкл/выкл (`-NoRestart`) |
| `local_users` | Локальные пользователи и группы, членство |

### 📎 Файлы, экран, веб

| Инструмент | Назначение |
|---|---|
| `download_file` | Загрузка по URL (белый список доменов, лимит размера) |
| `capture_screenshot` | Скриншот экрана для анализа |
| `read_clipboard` | Содержимое буфера обмена (текст/картинка) |
| `analyze_folder` | Обзор папки с текстом и изображениями |
| `search_web` | Веб-поиск |
| `scrape_url` | Чтение веб-страницы |
| `ask_user` | Явный вопрос с выбором |
| `change_rollback` | Снимки, сравнение и восстановление |

---

## 🛡️ Безопасность

- ✅ **Подтверждение опасных действий.** Запись в реестр, управление службами, изменения брандмауэра, установка/удаление ПО, очистка диска — только с явного согласия и с описанием, что именно изменится.
- 👁️ **Режим только для чтения** (`/readonly`) блокирует любые изменения — можно спокойно диагностировать.
- ↩️ **Откат** (`/undo`). Перед изменениями снимается снимок служб, задач и реестра. Отдельные операции (правила брандмауэра, компоненты Windows) откатываются вручную — программа честно предупреждает об этом в диалоге.
- 👤 **Защита учётных записей.** Нельзя отключить текущего пользователя или удалить последнего активного администратора.
- 🚫 **Запрет удаления файлов** через PowerShell и filesystem.
- 🔐 **Секреты.** Ключ берётся только из переменной окружения или user-secrets. Ключи BitLocker и пароли пользователей не выводятся.

---

## 📄 Отчёты

Команда `/export` сохраняет журнал сессии в **HTML**, **Markdown** или **PDF** — со всеми действиями и их полным выводом.

---

## ⚙️ Настройки

Файл `appsettings.json` рядом с `.exe` (без секретов):

| Параметр | Описание |
|---|---|
| `Session.Mode` | `continuous` или `isolated` |
| `Venice.Model` | Модель по умолчанию |
| `Venice.MaxToolRounds` | Предел шагов инструментов на запрос |
| `Download.MaxSizeMb` | Лимит размера загрузок |
| `Download.AllowedDomains` | Разрешённые домены для загрузок |

---

## 📜 Лицензия

Распространяется под лицензией **GNU General Public License v3.0**.

Вы можете свободно использовать, изменять и распространять программу при условии, что производные работы также остаются под GPL v3.0. Программа поставляется «как есть», без каких-либо гарантий.

Полный текст — в файле [LICENSE](LICENSE).

<div align="center">

---

Сделано для быстрой диагностики и починки Windows 🪟

</div>
